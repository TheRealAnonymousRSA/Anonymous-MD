# Anonymous-MD Pairing Site

Developer identity: `TheRealAnonymousRSA`

The web UI is served by the same Node process. Set `PORT` from the hosting provider.
The authenticated WhatsApp account is treated as the bot-instance owner; its number is not hard-coded into the public project.

Render:
- Build: `npm install`
- Start: `npm start`
- Set `DEVELOPER_NAME=TheRealAnonymousRSA`
